# Copy this file to backend/.env and fill in values before running the API.

# REQUIRED. Secret key used to sign admin JWT tokens (HS256).
# The server will not start if this is missing or blank.
# Generate a strong random value, for example:
#   openssl rand -hex 32
JWT_SECRET_KEY=
